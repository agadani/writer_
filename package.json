{
  "name": "writer-cf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public",
    "build": "node build.js",
    "deploy": "node build.js && npx wrangler pages deploy dist --project-name writer --branch production"
  }
}
